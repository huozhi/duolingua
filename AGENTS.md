# Agent rules

- Always use lowercase kebab-case for new or renamed files and directories.
- Keep ecosystem-mandated filenames unchanged when a tool requires an exact name, such as
  `AGENTS.md`, `Dockerfile`, `README.md`, `package.json`, or `app.zon`.
